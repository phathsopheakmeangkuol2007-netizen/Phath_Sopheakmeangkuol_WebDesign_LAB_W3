# Homework WebDesign Lab Week 3

## Homework : Build the layout shown below using either CSS Flexbox or CSS Grid.
Constraints:

- You may use CSS Flexbox, CSS Grid, or a combination of both.
- The left section should automatically fill the remaining horizontal space.
- The right section must have a width of 10em.
- Each box in the right section must have a height of 100px.

Tip (If you use Flexbox):
- display
- flex-direction
- flex-grow
- gap
- width
  
Tip (If you use CSS Grid) :
- display
- grid-template-columns
- grid-template-rows
- grid-column
- grid-row

Exercise 1 : Using CSS Grid, build the layout shown below.
Constraints:
- Create 3 columns with sizes: 200px 1fr 1fr
- Create 4 rows with sizes: 100px 200px 200px 100px
- Use a gap of 10px between all grid items.
- Do not use Flexbox or absolute positioning.
  
Tip: CSS properties that may be useful:
- display
- grid-template-columns
- grid-template-rows
- grid-column
- grid-row
- gap

Exercise 2 - Using Flexbox, build the layout shown below.
Constraints:
- The page must contain a Header, Main Content, and Footer.
- Set the Header height to 100px.
- Set the Footer height to 100px.
- The Main Content must automatically fill the remaining height.
- Add a 10px gap between each section.
- Do not use CSS Grid or absolute positioning.
  
Tip: CSS properties that may be useful:
- display
- flex-direction
- flex-grow
- gap
- height

